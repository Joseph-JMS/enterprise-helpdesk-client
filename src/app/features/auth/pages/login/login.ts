import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  selector: 'auth-login',
  imports: [RouterLink],
  templateUrl: './login.html',
})
export class Login { }
